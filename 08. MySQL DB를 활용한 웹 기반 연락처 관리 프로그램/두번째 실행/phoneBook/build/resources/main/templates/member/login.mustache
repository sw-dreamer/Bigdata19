<!DOCTYPE html>
<html lang="ko" xmlns:th="http://www.thymeleaf.org">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>로그인</title>
    <style>
        body {
            background-color: #f4f7fc;
            font-family: Arial, sans-serif;
        }
        .login-container {
            width: 350px;
            margin: 100px auto;
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.1);
        }
        h2 {
            text-align: center;
            margin-bottom: 20px;
        }
        .input-field {
            width: 100%;
            height: 40px;
            font-size: 15px;
            border: 1px solid #ddd;
            border-radius: 25px;
            margin-bottom: 15px;
            padding-left: 15px;
            background-color: #f1f1f1;
        }
        .btn {
            width: 100%;
            height: 40px;
            font-size: 16px;
            border: none;
            border-radius: 25px;
            background-color: #E5B933;
            color: white;
        }
        .btn:hover {
            background-color: #4a67c0;
        }
        .btn:active {
            background-color: #3a58b2;
        }
        .forgot {
            text-align: center;
            margin-top: 15px;
        }
        .forgot a {
            font-size: 14px;
            color: #5c7aef;
            text-decoration: none;
        }
        .forgot a:hover {
            text-decoration: underline;
        }
        .alert {
            width: 100%;
            margin-bottom: 15px;
            padding: 10px;
            font-size: 14px;
        }
    </style>
</head>

<body>
{{#msg}}
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        {{msg}}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
{{/msg}}

<div class="login-container">
    <h2>로그인 페이지</h2>

    <form action="/member/login" method="post">
        <input type="text" class="input-field" placeholder="ID" name="memberId" id="memberId" required>
        <input type="password" class="input-field" placeholder="Password" name="password" id="password" required>
        <button type="submit" class="btn">로그인</button>
    </form>

    <div class="forgot">
        <a href="/member/forgotId">아이디 찾기</a> |
        <a href="/member/forgotPassword">비밀번호 찾기</a>
    </div>

    <div class="text-center mt-3">
        <a href="/member/signup" style="font-size: 14px;">회원가입</a>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
</body>

</html>
